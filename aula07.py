# Variáveis → Uma referência a um local da memória do computador para o armazenamento de dados.

# PEP 8 → Guia de boas práticas em código Python.
# PEP8 em relação à variáveis: inicie variáveis com letras minúsculas. Pode usar números e underline (para separar palavras compostas).
# Ex: nome_variavel (snakeCase) = expressão
# o sinal "=" é o operador de atribuição.

nome_completo = ''
soma = 2 + 2
print(soma)

# os tipos primitivos (str, int, float e bool) se referem a valores literais 

# Uso de variáveis
# → Armazenar dados
# → Evitar repetições de um trecho de código ao alterar o mesmo. Com variáveis, a alteração é feita apenas em seu valor.

int_um = int('1')

# print(int('1'), type(int('1'))) → 1 <class 'int'>

print(int_um, type(int_um)) # 1 <class 'int'>

# Clean Code em relação à variáveis → Use nomes descritivos para suas variáveis, ou seja, um nome que explique o que foi colocado no valor.

# O valor de uma variável é obtido por meio do identificador (nome) da variável.
